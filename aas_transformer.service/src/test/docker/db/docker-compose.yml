version: '3'

services:
  aas-transformer-db:
    image: mariadb:10
    environment:
      MARIADB_USER: aas-transformer
      MARIADB_PASSWORD: password
      MARIADB_ROOT_PASSWORD: password
    ports:
      - 4044:3306